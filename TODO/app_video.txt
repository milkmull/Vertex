first:
    
    x rework enums
    x prune unused files
    - constexpr base64
    x compile on raspberry pi
    - rework hints

todo:

    - normalize internal video api to take and return pointers to display_instance instead of display_id
    x normalize macros, including feature flags and hints
    - hint descriptions
    x finish window events
    - finish video api
    - complete window class (front facing wrapper and internal video api)
    - keep some data initialized when calling init and quit? maye bet parent pointer in constructor?
    - finish input front ends
    x disable app subsystems based on compiler configuration
    
    - handle app callback mode and quitting, signals