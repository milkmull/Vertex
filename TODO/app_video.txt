
todo:

    x clipboard
    x drop events
    x message box api

    x make err setting noexcept again
    x consider removing input backend api in favor of having everything under video_impl
    x review add_hint_callback_and_default_value calls
    x make sure all init and quit functions are correct
    x all subsystems should have init data functions
    x dummy impl classes
    x message_box independant of video api
    x handle app callback mode
    x test callbacks will static and shared
    x VX_API does not have to mark function implementations
    x normalize # indents
    x clang format?
    - move video features macro outside of video folder
    x maybe make mouse, keyboard, clipboard, touch, pen optional and/or stack allocated, maybe remove submodules from data struct
    - clean up classes (public, private, const api)
    - finish front end api
