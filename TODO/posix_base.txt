TODO:

cmake test:
{
    x Define Vertex project
    x Configure output files for debug and release builds
    - Install cmake scrip
    - Installation
    x Define Sandbox project
    x Link Sandbox to Vertex, test both static and shared
    - Test including Vertex after installation
    - clang tidy, clang format
    - editorconfig
}

filesystem:
{
    - file info
    - permissions (get or set file permissions)
    
    - directory iterator
    - recursive directory iterator
    
    - path manipulators (absolute, canonical, relative)
    - equivalent (are 2 paths the same?)
    - current path (get or set the current path)
    - get temp directory path
    - get user directory

    - copy operators
    - create operators
    - remove operators
    - rename operators
    - filesystem space
}

os:
{
    - time
    
    - filesystem
    - file io

    - thread, mutex, atomic wrapers
    - process (argument parsing, environment variables, process dispatch, reading)

    - system memory
    - system info
    - locale (language, timezone, country)
}

cleanup and finalizaton for base library:
{
    formatting:
    {
        - macros to check if a particular os module is available
    }
}

filesystem tests:
{
    - get/set current path
    - temp directory
    - user folders

    - creation and info (file, directory, symlink, directory symlink, hard link)
    - get hard link count
    
    - last write time
    - file size

    - absolute
    - relative
    - equivalent
    - canonical

    - read symlink
    - update permissions

    - directory iterators
    
    - remove
    - create directories, remove_all
    - recursive directory iterator
    
    - copy file
    - copy symlink
    - copy
    
    - rename

    - space
    - is_empty
}

testing:
{
    - util/bit/bit
    - util/bit/endian
    
    - util/crypto/MD5
    - util/crypto/SHA1
    - util/crypto/SHA256
    
    - util/encode/base64

    - util/string/regex
    - util/string/string
    - util/string/string_cast
    - util/string/string_compare
    
    - os/random
    - util/random/algorithm
    - util/random/bernoulli_distribution
    - util/random/discrete_distribution
    - util/random/generate_canonical
    - util/random/normal_distribution
    - util/random/pcg
    - util/random/rng
    - util/random/uniform_int_distribution
    - util/random/uniform_real_distribution
    
    - util/time/time
    - util/time/timer
    - util/time/datetime
    - os/time
    
    - os/file
    - os/filesystem
    - os/path
    
    - os/thread
    - os/mutex
    - os/atomic
    - os/process
    
    - os/system_info
    - os/locale
    - os/system_memory
    
    - os/shared_library
}

finish base library:
{
    x formatting
    - test base library
    - document library
    - test cmake installation process (windows)
}